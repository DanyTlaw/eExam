<nav class="navbar navstyle navbar-default navbar-fixed-top" role="navigation" xmlns="http://www.w3.org/1999/html">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-left">
        <% if user_signed_in? then %>
        <li><%= link_to "Startseite", root_path %></li>
        <li><%= link_to "Studiengänge", course_of_studies_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Logout", destroy_user_session_path,:method => :delete, class: "btn btn-info" %></li>
        <% else %>
          <li><%= link_to "Login", new_user_session_path, class: "btn btn-info" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
